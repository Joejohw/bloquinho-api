# Domain model

The initial schema contains users, professionals, professional categories, and their many-to-many links.

- `id`: internal `BIGINT`, used in foreign keys.
- `publicId`: 21-character, non-sequential identifier generated by the API.

A public identifier is an address, not an authorization token. Access to private information must still be authorized.
